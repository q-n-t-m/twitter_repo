© MMXVII 

This is a twitter tool designed to track keywords and return analytics around context such as frequency tables.

The tool generates its output in csv format and its input is configurable to keywords and number of tweets.

There is more functionality in the pipeline.

All it needs is a twitter API which you can get from apps.twitter.com